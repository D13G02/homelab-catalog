frrk8s:
  enabled: false

frr-k8s:
  enabled: false
  prometheus:
    serviceMonitor:
      enabled: false

prometheus:
  scrapeAnnotations: false
  serviceMonitor:
    enabled: false
  podMonitor:
    enabled: false

configInline:
  address-pools:
    - name: default-pool
      protocol: layer2
      addresses:
        - 192.168.50.100-192.168.50.120